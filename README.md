
  # Dynamic Multi-Business UI

  This is a code bundle for Dynamic Multi-Business UI. The original project is available at https://www.figma.com/design/LelJFWmDrzTk2ZsME0M5K2/Dynamic-Multi-Business-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  